input {
  beats {
    port => 5044
  }
}

filter {
  json {
    source => "message"
    remove_field => ["beat", "host", "input_type", "message", "offset", "source", "type"]
  }
}

output {
  elasticsearch {
    hosts => ["http://35.198.117.124:9200"]
    index => "logstash-candidates"
    document_type => "candidates"
  }
}
